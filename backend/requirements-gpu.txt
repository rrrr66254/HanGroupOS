# GPU 영상 생성 패키지 (han setup-gpu / han update 에서 자동 설치)
# torch/torchvision은 CUDA 전용 인덱스가 필요하므로 별도 처리됨
# 설치: pip install -r requirements-gpu.txt

diffusers>=0.33.0
transformers>=4.40.0
accelerate>=0.30.0
imageio[ffmpeg]>=2.34.0
imageio-ffmpeg>=0.5.1
sentencepiece>=0.2.0
protobuf>=4.25.0
